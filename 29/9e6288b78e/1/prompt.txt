<ide_opened_file>The user opened the file /temp/readonly/Bash tool output (432sjx) in the IDE. This may or may not be related to the current task.</ide_opened_file>
I don't see the publish head cask

---

with the brew install cask I don't see `orpheus` CLI in my PATH

---

Split as orpheus-head (CLI formula) + orpheus-head-app (cask)