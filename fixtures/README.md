# Demo fixtures

These files are synthesized from the throwaway BIP39 test vector
`legal winner thank year wave sausage worth useful legal winner thank yellow`
and hold no real-world value. Regenerate them with:

```
cargo run -p orpheus-demo-fixtures
```

Do **not** place real wallet files here — add them elsewhere and pass the path
via `orpheus scan`.
